<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="@dimen/x2">


        <TextView
            android:id="@+id/item_notification_settings_text"
            style="@style/Body1.Primary"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            tools:text="Sleep Score"/>

        <include
            android:id="@+id/item_notification_settings_switch"
            layout="@layout/widget_switch"
            android:layout_width="@dimen/widget_switch_width"
            android:layout_height="@dimen/widget_switch_height"/>
    </LinearLayout>

    <View
        style="@style/Divider.Horizontal"
        android:layout_marginStart="@dimen/x2"/>

</LinearLayout>
