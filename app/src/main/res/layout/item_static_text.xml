<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical">

    <ImageView
        android:id="@+id/item_static_text_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/gap_outer"
        android:layout_marginEnd="@dimen/gap_outer"
        android:layout_centerVertical="true"
        android:src="@drawable/icon_settings_advanced"
        android:importantForAccessibility="no" />

    <TextView
        android:id="@+id/item_static_text_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/item_static_text_icon"
        android:layout_gravity="center_vertical"
        android:layout_centerVertical="true"
        android:gravity="start"
        android:textAppearance="@style/AppTheme.Text.Body.Medium"
        android:text="@string/app_name"/>

    <TextView
        android:id="@+id/item_static_text_detail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/item_static_text_title"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="@dimen/gap_small"
        android:layout_marginEnd="@dimen/gap_outer"
        android:layout_marginTop="@dimen/gap_medium"
        android:layout_marginBottom="@dimen/gap_medium"
        android:layout_centerVertical="true"
        android:gravity="end|center_vertical"
        android:textAppearance="@style/AppTheme.Text.Body.Medium"
        android:textColor="@color/text_dim"
        android:text="@string/app_name"
        android:singleLine="true"
        android:ellipsize="end" />

    <View
        android:id="@+id/item_static_text_divider"
        style="@style/Divider.Horizontal"
        android:layout_alignStart="@id/item_static_text_title"
        android:layout_below="@id/item_static_text_detail"
        android:layout_marginEnd="@dimen/gap_outer" />

</RelativeLayout>