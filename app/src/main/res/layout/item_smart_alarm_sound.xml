<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingLeft="@dimen/gap_medium"
    android:paddingRight="@dimen/gap_medium"
    android:paddingTop="@dimen/gap_medium"
    android:paddingBottom="@dimen/gap_medium">

    <ImageView
        android:id="@+id/item_smart_alarm_sound_checked"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="@dimen/gap_small"
        android:src="@drawable/radio_on"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/item_smart_alarm_sound_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@id/item_smart_alarm_sound_checked"
        android:textAppearance="@style/AppTheme.Text.Body"
        android:text="@string/app_name"/>

    <is.hello.sense.ui.widget.TintedProgressBar
        android:id="@+id/item_smart_alarm_sound_busy"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_alignParentEnd="true"
        android:visibility="gone"
        style="@android:style/Widget.Holo.ProgressBar.Small"/>

</RelativeLayout>