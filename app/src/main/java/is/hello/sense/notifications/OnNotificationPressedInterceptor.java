package is.hello.sense.notifications;

/**
 * {@link android.app.Activity} classes implementing with this will prevent launch of intended push notification.
 * Used to return to existing flow to prevent user from loss of progress.
 */

public interface OnNotificationPressedInterceptor {
}
