package is.hello.sense.ui.widget.timeline;

import android.support.annotation.NonNull;

import is.hello.sense.ui.animation.AnimatorContext;

public class TimelineFirstItemAnimator extends TimelineSimpleItemAnimator {
    public TimelineFirstItemAnimator(@NonNull AnimatorContext animatorContext, @NonNull Listener listener) {
        super(animatorContext, listener);
    }
}
